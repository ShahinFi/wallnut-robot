; Local overrides (this file is gitignored).
;
; Wi-Fi secrets live in `src/esp/wifi_secrets.h` (gitignored) to avoid
; command-line quoting issues (spaces, '&', etc).
;
[env:esp8266]
; upload_port = COM4
